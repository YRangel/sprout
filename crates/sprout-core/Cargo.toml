[package]
name = "sprout-core"
version = "0.4.8"
description = "Core library for sprout: rootfs model, ELF inspection, strategy selection, guest-link plumbing"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
thiserror.workspace = true
nix = { workspace = true, features = ["process", "signal", "fs"] }

[dependencies.sprout-preload]
path = "../sprout-preload"

[dev-dependencies]
tempfile = "3"
